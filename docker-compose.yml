networks:
  sts-net:
    driver: bridge

services:
  # ==========================================
  # Init Headscale config
  # ==========================================
  headscale-init:
    image: alpine:latest
    container_name: headscale_init
    volumes:
      - ./headscale/config:/etc/headscale
      - ./headscale/config/config.headscale.yaml:/tmp/config.headscale.yaml:ro
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    command: >
      sh -c "
      if [ ! -f /etc/headscale/config.yaml ]; then
        echo 'Init Headscale config...';
        cp /tmp/config.headscale.yaml /etc/headscale/config.yaml;
        PGPASS=$${POSTGRES_PASSWORD};
        sed -i s/__PASSWORD_PLACEHOLDER__/$$PGPASS/g /etc/headscale/config.yaml;
        echo 'Password injected successfully.';
      else
        echo 'Config already exists.';
      fi
      "
    networks:
      - sts-net

  # ==========================================
  # Headscale Server — Mesh VPN Control Plane
  # ==========================================
  headscale:
    image: headscale/headscale:latest
    container_name: headscale
    restart: always
    ports:
      - "0.0.0.0:${HS_PORT:-8080}:8080"
    volumes:
      - ./headscale/config:/etc/headscale
      - ./headscale/data:/var/lib/headscale
    command: serve
    depends_on:
      headscale-init:
        condition: service_completed_successfully
      postgres_uat:
        condition: service_healthy
    networks:
      - sts-net

  # ==========================================
  # PostgreSQL UAT
  # ==========================================
  postgres_uat:
    image: postgres:17-alpine
    container_name: postgres_uat
    restart: always
    ports:
      - "127.0.0.1:${PG_UAT_PORT:-5433}:5432"
    environment:
      POSTGRES_DB: STS
      POSTGRES_USER: userSTS
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    volumes:
      - ./data/pgdata_uat:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U userSTS -d STS"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - sts-net

  # ==========================================
  # PostgreSQL MASTER
  # ==========================================
  postgres_master:
    image: postgres:17-alpine
    container_name: postgres_master
    restart: always
    ports:
      - "127.0.0.1:${PG_MASTER_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: STS
      POSTGRES_USER: userSTS
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    volumes:
      - ./data/pgdata_master:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U userSTS -d STS"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - sts-net

  # ==========================================
  # PostgreSQL Exporters (Prometheus)
  # ==========================================
  postgres-exporter-master:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: sts-postgres-exporter-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      DATA_SOURCE_URI: postgres_master:5432/STS?sslmode=disable
      DATA_SOURCE_USER: userSTS
      DATA_SOURCE_PASS: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    networks:
      - sts-net
    restart: unless-stopped

  postgres-exporter-uat:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: sts-postgres-exporter-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      DATA_SOURCE_URI: postgres_uat:5432/STS?sslmode=disable
      DATA_SOURCE_USER: userSTS
      DATA_SOURCE_PASS: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # pgAdmin — Web UI สำหรับจัดการ DB
  # ==========================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin_web
    restart: always
    ports:
      - "${PGADMIN_BIND_IP:-0.0.0.0}:${PGADMIN_PORT:-5050}:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@yourdomain.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-PgAdminSecurePass123!}
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - ./pgadmin/servers.json:/pgadmin4/servers.json:ro
      - ./pgadmin/pgpass:/tmp/pgpass:ro
    depends_on:
      - postgres_uat
      - postgres_master
    networks:
      - sts-net

  # ==========================================
  # Backend Services — MASTER
  # ==========================================
  sts-noc-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-NOC
        CSPROJ_FILE: NOC.csproj
    container_name: sts-noc-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-alert-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-ALERT
        CSPROJ_FILE: STS-ALERT.csproj
    container_name: sts-alert-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-teleport-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: SCS-TELEPORT
        CSPROJ_FILE: Teleport/Teleport.csproj
    container_name: sts-teleport-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-dashboard-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-DASHBOARD
        CSPROJ_FILE: STS-DASHBOARD.csproj
    container_name: sts-dashboard-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
      - AlertOptions__AlertAPI=http://sts-alert-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-master-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-MASTER
        CSPROJ_FILE: Master.csproj
    container_name: sts-master-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-adm-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-ADM
        CSPROJ_FILE: ADM.csproj
    container_name: sts-adm-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Key=a-string-secret-at-least-256-bits-long
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
      - PathFile__UploadProfile=/app/UploadFile/UserProfile
    networks:
      - sts-net
    restart: unless-stopped

  sts-install-master:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-INSTALL
        CSPROJ_FILE: Install.csproj
    container_name: sts-install-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-ic-master:
    profiles:
      - ic
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-INVENTORY-CONTROL/IC
        CSPROJ_FILE: IC.csproj
    container_name: sts-ic-master
    depends_on:
      postgres_master:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_master;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-master:80
      - Jwt__Audience=http://sts-adm-master:80
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Backend Services — UAT
  # ==========================================
  sts-noc-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-NOC
        CSPROJ_FILE: NOC.csproj
    container_name: sts-noc-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-alert-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-ALERT
        CSPROJ_FILE: STS-ALERT.csproj
    container_name: sts-alert-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-teleport-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: SCS-TELEPORT
        CSPROJ_FILE: Teleport/Teleport.csproj
    container_name: sts-teleport-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-dashboard-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-DASHBOARD
        CSPROJ_FILE: STS-DASHBOARD.csproj
    container_name: sts-dashboard-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
      - AlertOptions__AlertAPI=http://sts-alert-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-master-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-MASTER
        CSPROJ_FILE: Master.csproj
    container_name: sts-master-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-adm-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-ADM
        CSPROJ_FILE: ADM.csproj
    container_name: sts-adm-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Key=another-long-secret-key-for-uat-256-bits
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
      - PathFile__UploadProfile=/app/UploadFile/UserProfile
    networks:
      - sts-net
    restart: unless-stopped

  sts-install-uat:
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-INSTALL
        CSPROJ_FILE: Install.csproj
    container_name: sts-install-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  sts-ic-uat:
    profiles:
      - ic
    build:
      context: .
      dockerfile: Dockerfile.backend
      args:
        PROJECT_PATH: STS-INVENTORY-CONTROL/IC
        CSPROJ_FILE: IC.csproj
    container_name: sts-ic-uat
    depends_on:
      postgres_uat:
        condition: service_healthy
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      - Jwt__Issuer=http://sts-adm-uat:80
      - Jwt__Audience=http://sts-adm-uat:80
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Frontend — sts-portal (Next.js)
  # ==========================================
  sts-portal-master:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: sts-portal-master
    environment:
      NODE_ENV: production
      PORT: "3000"
      NEXT_PUBLIC_BASE_PATH: /app
      NEXT_PUBLIC_BASE_APP_URL: http://localhost:${MASTER_HTTP_PORT:-82}/app
      NEXT_PUBLIC_API_DASHBOARD_SERVICE: /api_dashboard_service
      NEXT_PUBLIC_API_ADM_SERVICE: /api_adm_service
      NEXT_PUBLIC_API_MASTER_SERVICE: /api_master_service
      NEXT_PUBLIC_API_TELEPORT_SERVICE: /api_teleport_service
      NEXT_PUBLIC_API_INSTALL_SERVICE: /api_install_service
      NEXT_PUBLIC_API_NOC_SERVICE: /api_noc_service
      NEXT_PUBLIC_API_IC_SERVICE: /api_ic_service
      NEXT_PUBLIC_API_ALERT_SERVICE: /api_alert_service
    networks:
      - sts-net
    restart: unless-stopped

  sts-portal-uat:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: sts-portal-uat
    environment:
      NODE_ENV: production
      PORT: "3000"
      NEXT_PUBLIC_BASE_PATH: /uat
      NEXT_PUBLIC_BASE_APP_URL: http://localhost:81/app
      NEXT_PUBLIC_API_DASHBOARD_SERVICE: /api_dashboard_uat_service
      NEXT_PUBLIC_API_ADM_SERVICE: /api_adm_uat_service
      NEXT_PUBLIC_API_MASTER_SERVICE: /api_master_uat_service
      NEXT_PUBLIC_API_TELEPORT_SERVICE: /api_teleport_uat_service
      NEXT_PUBLIC_API_INSTALL_SERVICE: /api_install_uat_service
      NEXT_PUBLIC_API_NOC_SERVICE: /api_noc_uat_service
      NEXT_PUBLIC_API_IC_SERVICE: /api_ic_uat_service
      NEXT_PUBLIC_API_ALERT_SERVICE: /api_alert_uat_service
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Reverse Proxy — MASTER
  # ==========================================
  nginx-exporter-master:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: sts-nginx-exporter-master
    command:
      - '-nginx.scrape-uri=http://nginx-master:80/stub_status'
    depends_on:
      - nginx-master
    networks:
      - sts-net
    restart: unless-stopped

  nginx-master:
    image: nginx:alpine
    container_name: nginx-master
    ports:
      - "${MASTER_HTTP_PORT:-82}:80"
    volumes:
      - ./nginx.master.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - sts-adm-master
      - sts-noc-master
      - sts-alert-master
      - sts-teleport-master
      - sts-dashboard-master
      - sts-master-master
      - sts-install-master
      - sts-portal-master
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Reverse Proxy — UAT
  # ==========================================
  nginx-exporter-uat:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: sts-nginx-exporter-uat
    command:
      - '-nginx.scrape-uri=http://nginx-uat:80/stub_status'
    depends_on:
      - nginx-uat
    networks:
      - sts-net
    restart: unless-stopped

  nginx-uat:
    image: nginx:alpine
    container_name: nginx-uat
    ports:
      - "81:80"
    volumes:
      - ./nginx.uat.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - sts-adm-uat
      - sts-noc-uat
      - sts-alert-uat
      - sts-teleport-uat
      - sts-dashboard-uat
      - sts-master-uat
      - sts-install-uat
      - sts-portal-uat
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Jenkins — CI/CD Server
  # ==========================================
  jenkins:
    build:
      context: .
      dockerfile: Dockerfile.jenkins
    container_name: jenkins
    restart: always
    ports:
      - "0.0.0.0:${JENKINS_PORT:-9090}:8080"
    environment:
      JAVA_OPTS: "-Djenkins.install.runSetupWizard=false"
      JENKINS_OPTS: "--prefix="
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      PG_UAT_HOST: postgres_uat
      PG_UAT_PORT: 5432
      PG_UAT_NAME: STS
      PG_UAT_USER: userSTS
      PG_UAT_PASS: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
      PG_MASTER_HOST: postgres_master
      PG_MASTER_PORT: 5432
      PG_MASTER_NAME: STS
      PG_MASTER_USER: userSTS
      PG_MASTER_PASS: ${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    volumes:
      - jenkins_data:/var/jenkins_home
      - ./jenkins/init.groovy.d:/usr/share/jenkins/ref/init.groovy.d:ro
      - ./jenkins/jobs:/opt/jenkins-job-templates:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PWD}:/workspace
    user: root
    depends_on:
      - postgres_uat
      - postgres_master
    networks:
      - sts-net

  # ==========================================
  # Prometheus — Monitoring
  # ==========================================
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    ports:
      - "9090:9090"
    networks:
      - sts-net
    restart: unless-stopped

  # ==========================================
  # Backup
  # ==========================================
  backup:
    image: alpine:latest
    container_name: sts-backup
    volumes:
      - ./backups:/backups
      - ./backup/db-backup.sh:/scripts/db-backup.sh:ro
      - ./backup/db-restore.sh:/scripts/db-restore.sh:ro
      - ./backup/check-backup.sh:/scripts/check-backup.sh:ro
      - D:/backup_DB_STS/lastbackup:/backup-source:ro
    environment:
      - ConnectionStrings__ConnString=Host=postgres_uat;Port=5432;Database=STS;User Id=userSTS;Password=${POSTGRES_PASSWORD:-058c55af16a8410eb1eb5e94f79d073be235}
    networks:
      - sts-net
    restart: unless-stopped

volumes:
  pgadmin_data:
  jenkins_data:
  prometheus_data:
